'use client';

import styled from 'styled-components';

const Wrapper = styled.article`
  max-width: 50rem;
  min-height: calc(100vh - 20rem); // Height of header + breadcrumbs + footer
  margin: 0 auto;
  padding: 3rem 1rem;
`;

export default Wrapper;
